{
  "modid": "${modid}",
  "name": "${mod_name}",
  "author_list": ["${mod_author}"],
  "description": "${mod_description}",
  "dependencies": ["basemod", "stslib"],
  "credits": "",
  "version": "${mod_version}",
  "sts_version": "${sts_version}",
  "mts_version": "${mts_version}"
}